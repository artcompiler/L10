OVERVIEW

L10 is a language with SVG syntax and semantics. It parse and renders SVG
to and from ASTs formatted as JSON. The translation between ASTs and SVG is
almost a direct mapping of AST tag property to SVG element of that tag name,
with attributes given by other AST properties.

L8 is written in L0. 

BUILDING

To build:

   cd build
   ./make.sh

This produces the file:

   ../bin/l10c.swf


USING

To run:

    ./comp.sh some.l9

or

    avmshell ../bin/l10c.swf some.l9

where avmshell is the name of a recent build of Tamarin.


This produces the file:

    some.l9.l10

